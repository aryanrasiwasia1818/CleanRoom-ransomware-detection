# ---- Core analysis ----
numpy>=1.24            # vectorized entropy + feature math
scikit-learn>=1.3      # IsolationForest anomaly model (ML half of the ensemble)

# ---- Interfaces ----
fastapi>=0.110         # REST API that backs the dashboard
uvicorn[standard]>=0.29  # ASGI server for the API / static dashboard
rich>=13.7             # human-friendly CLI tables, trees and timelines

# ---- Dev / test ----
pytest>=8.0            # unit + integration tests
