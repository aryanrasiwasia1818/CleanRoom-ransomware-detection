"""Interface adapters — CLI and HTTP. These are the only layers that do I/O with
the outside world; they translate user intent into calls on the
:class:`~cleanroom.app.CleanRoomApp` facade and render its results.
"""
